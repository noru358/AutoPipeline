name: generic-media-gate

on:
  push:
  pull_request:

jobs:
  test-media-gate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: python -m unittest pipeline.test_media_gate
